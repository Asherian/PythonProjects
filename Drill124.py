"""
Your script will need to use Python 3, the Tkinter module, and the OS module.

Your script will need to use the listdir() method from the OS module to iterate
through all files within a specific directory.

Your script will need to use the path.join() method from the OS module to
concatenate the file name to its file path, forming an absolute path.

Your script will need to use the getmtime() method from the OS module to find
out the latest date the file has been created or last modified.

Your script will need to create a database to record the qualifying file and
corresponding modified time-stamp.

Your script will need to print each file ending with a “.txt” file extension
and its corresponding mtime to the console.

Your script will need to use the move() method from the Shutil module to cut
all qualifying files and paste them within the destination directory.
"""
from tkinter import *
import tkinter as tk
from tkinter import messagebox

import Drill122
import Drill123







if __name__ == "__main__":
    pass
